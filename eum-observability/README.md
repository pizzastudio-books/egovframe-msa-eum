# 관측 (19장)

지표 수집기와 대시보드는 실습용 구성입니다. 운영 환경에서는 기관에서 이미 사용 중인 지표 수집 및 시각화 도구에 연계하는 것이 일반적입니다(14.2와 같은 판단 기준을 따릅니다).

```bash
kubectl apply -f prometheus.yaml
kubectl apply -f grafana.yaml
```

대시보드는 <http://eum.local:8080/grafana>에서 확인할 수 있습니다. 실습 환경에서는 로그인 기능을 비활성화했습니다.

## 노트북에서 돌아가게 만든 것

서비스 넷, 데이터베이스, 브로커가 이미 3GB를 사용합니다(15장). 기본 설정으로 배포하면 메모리 제한을 초과합니다.

| 무엇 | 값 | 왜 |
| --- | --- | --- |
| 수집 주기 | 15초 | 짧을수록 메모리를 더 쓴다 |
| 보존 기간 | 2시간 | 기본값은 15일이다 |
| 수집기 상한 | 384Mi | 실측 사용량은 25Mi 안팎 |
| 대시보드 상한 | 256Mi | |
| 저장소 | `emptyDir` | 파드와 함께 사라진다. 운영에서는 볼륨(10.3) |

## 켜야 했던 것

애플리케이션에도 수정이 필요했습니다.

| 무엇 | 없으면 |
| --- | --- |
| `micrometer-registry-prometheus` 의존 | `/actuator/prometheus` 가 404 다 |
| `include: …,prometheus` | 같음 |
| `percentiles-histogram` | 응답 시간 분위수가 빈 값이다 — **대시보드는 그려지는데 선이 안 나온다** |
| 파드의 `prometheus.io/scrape` 표시 | 수집기가 그 파드를 안 긁는다 |

셋째는 특히 조용합니다. 화면은 정상적으로 표시되지만, 값이 출력되지 않습니다.
