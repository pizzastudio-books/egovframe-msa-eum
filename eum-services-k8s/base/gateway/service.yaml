apiVersion: v1
kind: Service
metadata:
  name: eum-gateway
  namespace: eum
  labels:
    app.kubernetes.io/name: eum-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: eum-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
