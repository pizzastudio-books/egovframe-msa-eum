apiVersion: v1
kind: Service
metadata:
  name: eum-payment
  namespace: eum
  labels:
    app.kubernetes.io/name: eum-payment
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: eum-payment
  ports:
    - name: http
      port: 80
      targetPort: http
