apiVersion: v1
kind: Service
metadata:
  name: eum-notification
  namespace: eum
  labels:
    app.kubernetes.io/name: eum-notification
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: eum-notification
  ports:
    - name: http
      port: 80
      targetPort: http
