# 증빙 첨부를 담을 볼륨(10.3).
#
# 파드를 여러 벌로 늘리려면 여러 노드에서 동시에 쓸 수 있어야 하므로
# ReadWriteMany 가 필요하다. 어떤 스토리지를 고를지는 10.3 에서 다룬다.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: eum-attachments
  namespace: eum
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
